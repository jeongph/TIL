<pre>
  /*
    chapter 57. 가장 큰 값 구하기

    간단함. 가장큰 값 구하는거임. 당연히 함수명도 max
    max(배열 변수);
  */

  $arr = range(1, 1000);  // range는 범위 내부를 모두 배열로 만들어줌
  echo "가장큰수 : ".max($arr);

</pre>
<hr>
<?php
/*
  chapter 57. 가장 큰 값 구하기

  간단함. 가장큰 값 구하는거임. 당연히 함수명도 max
  max(배열 변수);
*/

$arr = range(1, 1000);  // range는 범위 내부를 모두 배열로 만들어줌
echo "가장큰수 : ".max($arr);

?>
