<pre>
    $arr = range(1, 10, 3);
    echo "배열 arr의 값의 수는 : ".count($arr);
</pre>
<hr>

<?php

/*
 * chapter 19. 배열의 값 개수 확인하기
 *
 * 배열 안에 값이 몇 개가 있는지 개수를 알고싶을 때 사용
 * // count() 함수 사용
 * */

// count() 함수 사용 방법
// count(배열 변수명);

$arr = range(1, 10, 3);
echo "배열 arr의 값의 수는 : ".count($arr);

?>