<pre>
    /*
 * chapter 45. 변수의 존재 확인
 *
 * isset을 사용하는데 이게 다른 언어와 비슷하기도 하고,
 * 생각보다 유무 판단할 때 굉장히 많이 쓰임.
 * */

if( isset($str) )
{
    echo "변수 str is ok";
}
else
{
    echo "변수 str is non set";
}

echo '<br>';

$str = "String";
if( isset($str) )
{
    echo "변수 str is ok";
}
else
{
    echo "변수 str is non set";
}
</pre>
<hr>
<?php
/*
 * chapter 45. 변수의 존재 확인
 *
 * isset을 사용하는데 이게 다른 언어와 비슷하기도 하고,
 * 생각보다 유무 판단할 때 굉장히 많이 쓰임.
 * */

if( isset($str) )
{
    echo "변수 str is ok";
}
else
{
    echo "변수 str is non set";
}

echo '<br>';

$str = "String";
if( isset($str) )
{
    echo "변수 str is ok";
}
else
{
    echo "변수 str is non set";
}
?>
