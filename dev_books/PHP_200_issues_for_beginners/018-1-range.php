<pre>
    $num = range(1, 10);

    echo "<pre>";
    var_dump($num);
</pre>
<hr>
<?php

/*
 * chapter 18. 특정 범위의 수를 배열로 만들기.
 *
 * range()함수를 사용하면 범위를 정할 수 있다.
 *
 * range() 함수 사용법
 * // range(시작하는 수, 끝나는 수);
 * // ex> range(1, 10); // 1부터 10까지의 배열
 * */

$num = range(1, 10);

echo "<pre>";
var_dump($num);

?>