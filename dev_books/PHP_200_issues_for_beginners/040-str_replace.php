<pre>
    /*
 * chapter 40. 문자열의 특정 문자 치환
 *
 * str_replace를 사용하는데, 다른 언어와 비슷한데 좀 다르다.
 * */
$str = "welcom to everdevel";

// 변수의 everdevel을 치환
$changeWord = str_replace( 'everdevel', 'beanscent', $str);
echo $changeWord;
</pre>
<hr>
<?php
/*
 * chapter 40. 문자열의 특정 문자 치환
 *
 * str_replace를 사용하는데, 다른 언어와 비슷한데 좀 다르다.
 * */
$str = "welcom to everdevel";

// 변수의 everdevel을 치환
$changeWord = str_replace( 'everdevel', 'beanscent', $str);
echo $changeWord;
?>
